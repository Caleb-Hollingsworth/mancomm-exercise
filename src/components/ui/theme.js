import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            purple: '#6b76cb',
            main: '#f0f3f8',
            hoverGray: '#f7f8fd',
            outlineGray: '#e2e4f4'
        },
        common: {
            black: '#393D54'
        }
    }
});